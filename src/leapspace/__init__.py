"""LeapSpace — the self-evolving app environment and evaluation harness.

The code lives in the ``leapspace.app_space`` submodule (actor, task config,
action lint, and the scenario apps). Ships with the leapflow distribution
(importable via the editable install); heavier dependencies live in the root
``pyproject.toml`` under the ``leapspace`` dependency group
(``make space-sync``).
"""
